@{
    var json =
        Convert.ToString(TempData["Toast"]);

    // var json =
    //     toast == null
    //     ? null
    //     : JsonSerializer.Serialize(toast);
}

@if (json != null)
{
    <script id="toast-data"
            type="application/json">
        @Html.Raw(json)
    </script>
}